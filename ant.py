class Ant:

    #global x, y, and mode
    
    def __init__(self,x,y,home):
        self._x = x # move these?
        self._y = y
        self._mode = 0
        self._home = home


    def status(self,mode,x,y)
        self._mode = mode
        self._x = x # move these?
        self._y = y

    lastloc = 
    
    def move()
        #random direction, except backwards



# Ant.status.self._mode ??? is that how that works?
# if it is, it should return the mode the ant is in.
